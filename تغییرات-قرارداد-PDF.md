# یکپارچه‌سازی چاپ/PDF قرارداد هنرجویی

همه‌ی فایل‌ها raw هستن (نه patch/diff) — مسیرشون دقیقاً همون مسیر داخل ریپو هست، کافیه جایگزین/اضافه کنی.

---

## چی پیدا شد (قبل از دست زدن به چیزی)

دنبال «همه‌ی جاهایی که قرارداد چاپ می‌کنن» گشتم و **۴ پیاده‌سازی جدا** پیدا شد، نه یکی — و هیچ‌کدوم دقیقاً شبیه هم نبودن:

1. **صفحه‌ی موفقیت ثبت‌نام (ویزارد)** — یک `<script>` داخل `SuccessStep.astro` که یه پنجره باز می‌کرد و `window.print()` می‌زد. این اصلاً PDF واقعی نبود؛ کاربر باید خودش از دیالوگ پرینت مرورگر «Save as PDF» رو دستی انتخاب می‌کرد. این اسکریپت با یه ترفند (`capture phase` + `stopImmediatePropagation`) قبل از کنترلر اصلی اجرا می‌شد و **یه پیاده‌سازی دوم و کاملاً مرده** رو داخل `RegistrationController.ts` (متد `printContract` قدیمی) به‌طور خاموش بی‌اثر می‌کرد — یعنی اون کد اصلاً هیچ‌وقت اجرا نمی‌شد، فقط اونجا مونده بود.
2. **پنل ادمین + پورتال هنرجو** — هر دو به `/api/admin/contract-generate` وصل بودن. این یکی واقعاً PDF واقعی می‌ساخت (از طریق `env.BROWSER`، همون مکانیزمی که برای گواهی‌نامه‌ها استفاده شده)، ولی با یه template HTML کاملاً متفاوت: بدون لوگو، هدر ساده، و کد پیگیری‌ای که نمایش می‌داد **بدون اصلاح bidi** بود — دقیقاً همون باگ RTL که برای موبایل و مبلغ شهریه قبلاً فیکس شده بود، ولی اینجا فراموش شده بود.
3. **یه template چهارم و کاملاً مرده** داخل `POST /api/admin/registrations` — چک کردم، هیچ‌جای کد این endpoint رو صدا نمی‌زد (فقط GET و PATCH استفاده می‌شدن). حذفش کردم.

نتیجه: ۴ ظاهر متفاوت برای یه سند «واحد»، و فقط ۲ تا از ۴ تا اصلاً PDF واقعی بودن.

## چی ساخته شد

**یک template + یک pipeline مشترک**، دقیقاً هم‌شکل با چیزی که برای گواهی‌نامه‌ها ساخته شده بود (`src/server/certificates/`):

- `src/server/contracts/template.ts` — تابع خالص `buildContractHtml`. طراحی رو از بین اون ۳ نسخه، **بهترین/آخرین نسخه‌ی تکامل‌یافته** (همونی که تو `SuccessStep.astro` بود — قاب طلایی دوتایی، لوگو، fit تک‌صفحه‌ای A4) رو انتخاب و به سرور منتقل کردم؛ چیزی رو از نو طراحی نکردم.
- `src/server/contracts/generate.ts` — لود از D1 (با `registration_id` یا `tracking_code`) + ساخت PDF واقعی از طریق همون `env.BROWSER.quickAction("pdf", ...)`.
- کد پیگیری (`FM-2026-XXXXXX`) حالا تو هدر هر ۳ سند نمایش داده می‌شه، با `direction:ltr; unicode-bidi:embed` ایزوله شده — همون فیکس bidi که برای موبایل/شهریه بود، این‌بار درست روی کد پیگیری هم اعمال شده.

**دو تا route، با دو مدل احراز هویت جدا:**

- `/api/admin/contract-generate` — بدون تغییر در URL/رفتار برای پنل ادمین و پورتال هنرجو (همون session-check قبلی). فقط دیگه template خودش رو نداره، از pipeline مشترک استفاده می‌کنه.
- `/api/contract-pdf` — **جدید**، عمومی (بدون لاگین)، فقط با `tracking_code`. این برای صفحه‌ی موفقیتِ خودِ ویزارده: درست بعد از ثبت‌نام موفق، مرورگر کد پیگیری رو داره ولی هیچ session ای (نه ادمین، نه هنرجو) نداره. **دونستن کد پیگیری خودش اعتبارسنجیه** — دقیقاً مثل کد پیگیری سفارش تو فروشگاه‌های آنلاین: سرور تولیدش می‌کنه، یکتاست، و فقط به همون کسی که همین الان ثبت‌نام کرده نشون داده می‌شه. این در واقع **سخت‌گیرانه‌تر** از قبله، نه ضعیف‌تر — قبلاً همین صفحه بدون هیچ چک سروری، مستقیم از state سمت کلاینت قرارداد رو نشون می‌داد.

⚠️ **این یه تصمیم معماری‌ایه که خواستم صریح بهت بگم**: `/api/contract-pdf` عمداً بدون لاگین کار می‌کنه چون در لحظه‌ی موفقیت ثبت‌نام هنوز session ای وجود نداره. اگه با این مدل راحت نیستی و ترجیح می‌دی هنرجو همون‌جا لاگین کنه یا از راه دیگه‌ای احراز بشه، بگو تا عوضش کنم — ولی به نظرم سطح دسترسی دقیقاً هم‌تراز چیزیه که همین الان (بدون هیچ چکی) وجود داشت.

## کد مرده‌ای که حذف شد

- `RegistrationController.ts`: متد قدیمی و غیرقابل‌اجرای `printContract` (iframe + print) → جایگزین شد با پیاده‌سازی واقعی (فراخوانی `/api/contract-pdf`، باز کردن نتیجه دقیقاً به همون روشی که دکمه‌ی پنل ادمین استفاده می‌کنه: تب خالی رو سریع باز می‌کنه تا popup-blocker مشکل نسازه، بعد PDF رو جاش می‌ذاره).
- `SuccessStep.astro`: اسکریپت capture-phase که کنترلر رو خاموش می‌کرد → کامل حذف شد.
- `admin/registrations.ts`: POST handler مرده + import بلااستفاده‌ی `buildContract` → حذف شد.
- `RegistrationRenderer.renderContract()`: divهای `.print-contract-header`/`.print-contract-footer` → حذف شدن. با grep تو کل ریپو مطمئن شدم این کلاس‌ها **هیچ CSSای نداشتن** — یعنی روی صفحه‌ی زنده هم بدون استایل رندر می‌شدن (لوگو با سایز اصلی فایل، ۴۵۰۰×۴۵۸۸ پیکسل، بدون هیچ محدودیتی!). این بخش فقط برای اسکریپت حذف‌شده لازم بود.

## فایل‌های تغییرکرده/جدید

```
src/server/contracts/template.ts          [جدید]
src/server/contracts/generate.ts          [جدید]
src/pages/api/contract-pdf.ts             [جدید]
src/pages/api/admin/contract-generate.ts  [تغییر — delegate به pipeline مشترک]
src/pages/api/admin/registrations.ts      [تغییر — حذف POST مرده]
src/components/registration/steps/SuccessStep.astro  [تغییر — حذف اسکریپت shadow]
src/scripts/registration/RegistrationController.ts   [تغییر — printContract واقعی]
src/scripts/registration/RegistrationRenderer.ts      [تغییر — حذف header/footer بی‌استایل]
wrangler.jsonc                            [تغییر — کامنت BROWSER دیگه فقط مال گواهی‌نامه نیست]
doc/ADR/ADR-013 — Contract PDF Unification.md  [جدید — جزئیات فنی کامل‌تر اینجاست]
```

## تست‌هایی که زدم

- `npx astro check`: قبل از تغییرات ۶۴ خطا (روی ریپوی دست‌نخورده)، بعدش ۵۹ تا — یعنی خالص بهتر شده، نه بدتر. هرچی باقی مونده تو فایل‌های من، همون الگوی از قبل‌موجود تو `certificates/generate.ts` هست (D1 با `.first<T>()`)، چیزی که خودم اضافه نکردم.
- `npx vitest run`: هر ۷۰ تست سبز (شامل ۲۰ تای `ContractTemplates.test.ts` — به `buildContract` دست نزدم).
- `npm run build`: کامل build می‌شه (فقط قدم آخر wrangler که نیاز به `CLOUDFLARE_API_TOKEN` داره تو sandbox من fail می‌شه، ربطی به کد نداره).
- خروجی HTML رو با داده‌ی واقعی تست کردم (تگ‌های balanced، بدون `undefined`/`NaN`، کد پیگیری چپ‌به‌راست درست نمایش داده می‌شه).
- یه رندر تصویری تقریبی هم گرفتم (`wkhtmltoimage` — همون ابزاری که تو ADR-012 هم برای گواهی‌نامه استفاده شده بود) که اول نگران‌کننده به نظر رسید (متن راست‌چین بریده می‌شد)، ولی با یه تست ایزوله ثابت شد این باگ خودِ موتور قدیمی WebKit این ابزاره (با RTL + justify مشکل داره) — نه مشکل CSS من. کروم واقعی (که `env.BROWSER` واقعاً ازش استفاده می‌کنه) این مشکل رو نداره؛ همین الان هم داره همین CSS رو درست رندر می‌کنه، چون از قبل تو مرورگر واقعی کاربرا (window.print) استفاده می‌شده.

## نصب

هیچ migration جدید، هیچ binding جدید، هیچ dependency جدیدی لازم نیست — `BROWSER` از قبل تو `wrangler.jsonc` تنظیم بود (برای گواهی‌نامه‌ها). فقط فایل‌ها رو جایگزین/اضافه کن و طبق روال همیشگی build/deploy بزن.

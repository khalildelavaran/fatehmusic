# تغییرات این بسته

همه فایل‌ها raw هستن (نه patch) — چون محیط ویندوزی با CRLF مشکل پچ داره.
مسیر هر فایل داخل zip دقیقاً همون مسیرش توی ریپو هست؛ فقط جایگزین کن.

**قبل از دیپلوی: migration جدید رو اجرا کن** (`npm run db:migrate:local` برای تست، بعد `npm run db:migrate:remote`) — بدون این migration فرم ثبت‌نام کار نمی‌کنه چون ۵ فیلد جدید به دیتابیس اضافه شده.

---

## ۵. قرارداد هنرجویی خودکار (جدیدترین بخش)

### فایل‌های جدید
- `migrations/0008_add_contract_fields.sql`
- `src/scripts/registration/ContractTemplates.ts` + `.test.ts`

### فایل‌های تغییر یافته برای این بخش
`RegistrationStore.ts`, `StudentStep.astro`, `RegistrationValidation.ts`, `RegistrationController.ts`, `RegistrationRenderer.ts`, `SuccessStep.astro`, `registration.css`, `src/pages/api/register.ts`

### ۵ فیلدی که اضافه شد (چون در قرارداد بودن ولی در فرم ثبت‌نام نبودن)
نام پدر، شغل، متولد سال (شمسی)، محل صدور کدملی، آدرس — همه در مرحله «مشخصات هنرجو» اضافه شدن، و اجباری‌ان (چون قرارداد بدون‌شون ناقصه).

### انتخاب خودکار نوع قرارداد
دو متن قرارداد آپلود شده رو مقایسه کردم: تنها تفاوت‌شون بند «جلسات/مدت» در ماده ۲ (۸ جلسه/دو ماه در برابر ۱۰ جلسه/ده هفته متوالی) و بند غیبت در ماده ۴(ب) هست. برای تشخیص اینکه کدوم دوره کدوم قرارداد رو می‌گیره، از `coursePricingMap` در `src/data/pricing.js` استفاده کردم (همون چیزی که همین الان روی مرحله «بازبینی» قیمت رو نشون می‌ده) — نه یک لیست جدا از سازهای کلاویه‌ای. تعداد جلسات همون‌جا (۸ و ۱۰) دقیقاً با دو متن قرارداد مطابقت داره، که خیالم رو راحت کرد این منبع درستیه.

### چیزهایی که خودکار پر می‌شه
نام، خانم/آقای (از جنسیت)، کدملی، شماره همراه، نام پدر، محل صدور، متولد سال، شغل، آدرس، رشته (از روی دوره انتخاب‌شده، با حذف پیشوند «آموزش»/«دوره»)، **مبلغ قرارداد** (از `pricing.js`، تبدیل خودکار تومان→ریال ضربدر ۱۰، چون قرارداد صراحتاً «ریال» خواسته)، مبلغ به حرف (تابع عدد‌به‌حروف فارسی نوشتم و تست کردم)، و تاریخ ماده ۶/امضاء (تاریخ شمسی امروز).

### دو فیلدی که عمداً خالی گذاشتم — این‌ها نیاز به نظر تو دارن
- **ترم**: سیستم هیچ اطلاعی از اینکه هنرجو ترم اولشه یا ترم چندمه نداره (نه لاگین داره، نه سابقه ثبت‌نام قبلی رو چک می‌کنه). گذاشتمش خالی برای پرسنل. اگه همیشه ترم ۱ درسته (هنرجوی برگشتی از همین فرم ثبت‌نام نمی‌کنه)، بگو تا خودکار «۱» بذارم.
- **تاریخ شروع کلاس (ماده ۲)**: این به تقویم واقعی کلاس‌های آموزشگاه بستگی داره که جایی توی سیستم ذخیره نمی‌شه (فقط روز هفته مثل «شنبه‌ها» داریم، نه تاریخ دقیق شروع ترم). گذاشتمش خالی هم.

هر دو دقیقاً مثل متن اصلی قرارداد، با نقطه‌چین خالی می‌مونن؛ چیزی حدس زده نشده.

### نمایش و چاپ
قرارداد در پایان ثبت‌نام (مرحله موفقیت، همون‌جایی که «چاپ رسید» هست) نشون داده می‌شه، با دکمه‌ی جدای «چاپ قرارداد» که فقط خود قرارداد رو چاپ می‌کنه (نه بقیه‌ی صفحه). برعکسش هم درست شد: چاپ رسید معمولی دیگه قرارداد رو داخلش نمی‌آره.

### تست شد
- منطق انتخاب قرارداد + تابع عدد‌به‌حروف: ۱۳ تست جدید Vitest
- خروجی واقعی قرارداد رو برای یک دوره‌ی کلاویه‌ای و یک دوره‌ی عادی چاپ گرفتم و دستی خوندم — متن، مبلغ، و بند غیبت هرکدوم درست بود
- migration رو روی D1 محلی واقعی اجرا کردم (نه فقط syntax-check) و یک INSERT کامل با همون ستون‌هایی که register.ts می‌فرسته تست کردم — مقادیر درست ذخیره و خونده شدن

---

## ۱. جریان ثبت‌نام (auto-skip + لینک مستقیم)

فایل‌ها: `RegistrationController.ts`, `RegistrationRenderer.ts`, `RegistrationStore.ts`, `RegistrationUtils.ts`+test, `RegistrationFlow.astro`, `InstrumentStep.astro`+`InstrumentCard.astro`, `registration.css`, `courses/[slug].astro`, `instructors/[slug].astro`

اگه دوره فقط ۱ استاد فعال داشته باشه، یا استاد فقط ۱ زمان داشته باشه، بدون کلیک انتخاب می‌شه و می‌ره مرحله بعد (محتوای واقعی مرحله همچنان رندر می‌مونه برای دکمه‌ی «بازگشت»). نوار «ساز · استاد · زمان» همیشه نشون می‌ده چی برای هنرجو تعیین شده. `/register?course=slug` و `/register?instructor=slug` مراحل رو بر همون اساس کم می‌کنن. روی داده واقعی: از ۲۳ دوره، ۲۰ تا ۱ استاد دارن؛ از ۱۴ استاد، ۹ تا ۱ زمان دارن.

## ۲. یکپارچه‌سازی استایل گواهینامه/کتاب

فایل‌ها: `certificates.astro`, `certificates.css` (admin), `books.astro`

باکس جستجوی `/admin/certificates` حالا از `.admin-form` مشترک استفاده می‌کنه (قبلاً radius و افکت فوکوسش با بقیه‌ی سایت فرق داشت). برچسب بالای `/admin/books` که اشتباهاً «گواهینامه‌ها» بود (کپی‌پیست) شد «مدیریت کتاب‌ها».

نکته: `AGENTS.md` به پوشه‌ی `docs/` اشاره می‌کنه ولی این پوشه اصلاً وجود نداره — چیزی نساختم چون نمی‌خواستم محتوا حدس بزنم.

## ۳. تشخیص خودکار کد ملی در فرم گواهینامه

فایل‌ها: `certificates.js`, `certificates.astro` (admin)

با انتخاب هنرجو از جستجو، فیلد «کد ملی هنرجو» از `student_national_code` همون ثبت‌نام پر می‌شه، با یادداشت کوچیک زیرش.

## ۴. بج ثبت‌نام روی کارت‌های دوره و استاد

فایل‌ها: `Courses.astro`+`courses.css`, `Instructors.astro`+`instructors.css`

هر کارت از یک `<a>` بزرگ به یک `<div>` با سه لینک جدا تبدیل شد (عکس، عنوان، بج ثبت‌نام) چون یک `<a>` نمی‌تونه `<a>` دیگه‌ای داخلش داشته باشه. بج «ثبت‌نام» گوشه‌ی پایین‌چپ عکسه (هم‌فاصله با بج «مدیر آموزشگاه»)، پیش‌فرض شیشه‌ای نیمه‌شفافه و روی هاور طلایی می‌شه.

---

## تأیید نهایی (بعد از هر ۵ بخش)

- `npm run test` → ۶۱/۶۱ تست
- `npm run check` → همون ۹ خطای از قبل موجود و بی‌ربط (worker.ts, login.astro, admin-auth.ts, یک fbq قدیمی)، صفر خطای جدید
- `npm run build` → کامل و بدون خطا
- migration روی D1 محلی واقعی اجرا و تأیید شد

`package-lock.json` توی این zip نیست.

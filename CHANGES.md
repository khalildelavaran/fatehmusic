# تغییرات این بسته

همه فایل‌ها raw هستن (نه patch) — چون محیط ویندوزی با CRLF مشکل پچ داره.
مسیر هر فایل داخل zip دقیقاً همون مسیرش توی ریپو هست؛ فقط جایگزین کن.

**قبل از دیپلوی: دو migration رو به ترتیب اجرا کن** (`npm run db:migrate:local` برای تست، بعد `npm run db:migrate:remote`).

---

## ⚠️ یک نکته که مطمئن نیستم — لطفاً چک کن

توی بخش ۷، برای امضاء گفتی «امضا استاد سمت راست، امضا مدیر آموزشگاه سمت چپ». ولی خود متن قرارداد (ماده ۱ و ماده ۶) دو طرف قرارداد رو **هنرجو** و **آموزشگاه** تعریف کرده، نه استاد. فکر کردم شاید منظورت همون «هنرجو» بوده (شاید موقع تایپ با توجه به بحث‌های قبلی درباره‌ی استادها اشتباه شده). فعلاً **هنرجو رو سمت راست، آموزشگاه رو سمت چپ** گذاشتم — دقیقاً مطابق طرف‌های قرارداد. اگه واقعاً منظورت استاد بود (یعنی می‌خوای استادِ همون هنرجو هم امضا کنه، جای هنرجو یا علاوه بر آن)، بگو تا عوض کنم.

---

## ۷. تاریخ شروع، سن از روی سال تولد، راست‌چین (جدیدترین بخش)

### فایل‌های تغییر یافته
`ContractTemplates.ts`+test, `RegistrationUtils.ts`, `format-date.ts`+test (جدید), `RegistrationController.ts`, `StudentStep.astro`, `RegistrationValidation.ts`, `registration.css`

### تاریخ شروع کلاس (ماده ۲) — دیگه خالی نیست
دقیقاً طبق قانونی که گفتی: اولین تکرار روزِ انتخابی هنرجو، از تاریخ ثبت‌نام به بعد. مثال خودت رو مستقیم تست کردم (ثبت‌نام شنبه + انتخاب پنجشنبه = پنجشنبه همون هفته، ۵ روز بعد) — دقیقاً درست. یک حالت مرزی هم اضافه کردم: اگه روزِ انتخابی هنرجو توی همون هفته گذشته باشه (مثلاً ثبت‌نام چهارشنبه ولی روز کلاس شنبه)، به‌جای رفتن به گذشته، جلو می‌ره به تکرار بعدی. تست شد.

### سن ↔ سال تولد
فیلد «سن» رو از فرم حذف کردم؛ الان فقط «متولد سال» پرسیده می‌شه و سن خودکار محاسبه می‌شه (سال شمسی جاری − سال تولد) و دقیقاً مثل قبل توی دیتابیس و پنل ادمین ذخیره/نمایش داده می‌شه — هیچ‌جای دیگه‌ای تغییر نکرد، فقط منبع مقدار عوض شد.

### راست‌چین
کل کارت قرارداد (`direction:rtl; text-align:right`) صریح شد — قبلاً به‌صورت ضمنی از تنظیم کلی صفحه ارث می‌برد که برای محتوای چاپی مطمئن‌تر نبود. ردیف امضاء هم صریحاً `direction:rtl` گرفت تا مستقل از هر بازنشانی احتمالی در حالت چاپ درست بمونه.

### تست شد
- ۴ تست جدید برای محاسبه‌ی تاریخ شروع (شامل مثال دقیق خودت + حالت مرزی)
- ۲ تست برای تابع سال شمسی جاری
- خروجی کامل قرارداد رو دوباره چاپ گرفتم و دستی خوندم — ترم، تاریخ شروع، و تاریخ امضاء همه با هم و با تاریخ واقعی امروز هم‌خوان بودن

---

## ۶. شماره ترم خودکار

بر اساس قانونی که گفتی: هر ثبت‌نام یک ترمه؛ اگه هنرجو (بر اساس کدملی) سابقه‌ی ثبت‌نام نداشته باشه می‌شه ترم ۱، ثبت‌نام بعدیش می‌شه ترم ۲. فایل جدید: `migrations/0009_add_term_number.sql`. سرور موقع ثبت‌نام تعداد ثبت‌نام‌های قبلیِ همون کدملی رو می‌شمره؛ برای داده‌های قدیمی هم بر اساس تاریخ واقعی هر ثبت‌نام (نه یک عدد ثابت) عقب‌گرد شماره‌گذاری شد — روی D1 محلی با داده‌ی واقعی تست شد.

## ۵. قرارداد هنرجویی خودکار

فایل جدید: `migrations/0008_add_contract_fields.sql`, `ContractTemplates.ts`+test. ۵ فیلد جدید (نام پدر، شغل، محل صدور کدملی، آدرس، متولد سال) به فرم اضافه شد. نوع قرارداد (استاندارد/کلاویه‌ای) از `coursePricingMap` تشخیص داده می‌شه — همون منبعی که برای قیمت‌گذاری استفاده می‌شه. مبلغ و مبلغ‌به‌حرف خودکار (تومان ذخیره‌شده × ۱۰ = ریال، چون قرارداد صراحتاً «ریال» خواسته). نمایش و چاپ در پایان ثبت‌نام، با دکمه‌ی چاپ جدا از چاپ رسید.

## ۱-۴ (خلاصه، جزئیات در نسخه‌های قبلی این فایل)

۱) auto-skip مراحل ثبت‌نام + لینک مستقیم `?course=`/`?instructor=` · ۲) یکپارچه‌سازی استایل گواهینامه/کتاب · ۳) تشخیص خودکار کد ملی در فرم گواهینامه · ۴) بج ثبت‌نام روی کارت‌های دوره و استاد

---

## تأیید نهایی (بعد از هر ۷ بخش)

- `npm run test` → ۶۹/۶۹ تست
- `npm run check` → همون ۹ خطای از قبل موجود و بی‌ربط، صفر خطای جدید
- `npm run build` → کامل و بدون خطا
- هر دو migration روی D1 محلی واقعی اجرا و با داده‌ی واقعی تست شدن

`package-lock.json` توی این zip نیست.

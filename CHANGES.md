# تغییرات این بسته

همه فایل‌ها raw هستن (نه patch) — چون محیط ویندوزی با CRLF مشکل پچ داره.
مسیر هر فایل داخل zip دقیقاً همون مسیرش توی ریپو هست؛ فقط جایگزین کن.

**قبل از دیپلوی: دو migration رو به ترتیب اجرا کن** (`npm run db:migrate:local` برای تست، بعد `npm run db:migrate:remote`).

---

## ۸. بهبود پرینت قرارداد (جدیدترین بخش)

### ۱) متن بالای صفحه حذف شد
اون متن («ساز مورد علاقه و استاد خود را انتخاب کرده...») مال یک بخش دیگه‌ست: تیتر/زیرتیتر بالای کل صفحه‌ی `/register` (توی `register.astro`، خارج از خود ویزارد ثبت‌نام). چون این بخش (`.registration-hero`) توی لیست چیزهایی که موقع چاپ قرارداد مخفی می‌شن نبود، همراه قرارداد چاپ می‌شد. اضافه‌ش کردم به لیست مخفی‌ها.

### ۲) شماره موبایل معکوس
این یه باگ RTL بود، نه چیزی که دستی وارد شده باشه. شماره رو با خط‌تیره گروه‌بندی می‌کردم (مثل ۰۹۱۶-۶۰۴-۰۷۵۷) و چون قرارداد صریحاً `direction:rtl` داره، مرورگر این گروه‌های جداشده با خط‌تیره رو برعکس نمایش می‌ده (الگوریتم bidi یونیکد). حالا هم شماره موبایل و هم مبلغ قرارداد (که با کاما جدا می‌شد و همین ریسک رو داشت) به‌صورت رقم‌های فارسیِ پشت‌سرهم و بدون جداکننده نمایش داده می‌شن — دقیقاً مثل کدملی.

### ۳) «اطلاق» / «اطالق»
سورس خودم رو چک کردم — «اطلاق» درست نوشته شده (خط ۱۸۴ همون فایل). حتی اسم خودت («دلاوران») هم توی متنی که برام فرستادی به همین شکل جابه‌جا شده بود («دالوران»)، که نشون می‌ده این یه مشکل تایپی نیست، بلکه یه اتفاق شناخته‌شده در PDF فارسیه: وقتی از خروجی چاپ/PDF مرورگر متن رو کپی می‌کنی (نه وقتی مستقیم روی صفحه/کاغذ می‌بینی)، حروفی که با «لا» (لام-الف) ترکیب می‌شن گاهی توی متنِ کپی‌شده جابه‌جا می‌شن — خودِ نمایش چاپی معمولاً درسته. لطفاً پرینت واقعی (یا پیش‌نمایش چاپ) رو چک کن، نه متنی که کپی کردی؛ اگه اونجا هم اشتباه بود بگو دوباره نگاه کنم.

### ۴) یک صفحه
فونت و فاصله‌ها رو مخصوص حالت چاپ (جدا از نمایش روی صفحه) فشرده‌تر کردم (`@page` با حاشیه ۱۲ میلی‌متر، فونت حدود ۹.۵pt، فاصله‌ی کمتر بین مواد) تا کل قرارداد (۶ ماده + امضاء) در یک صفحه‌ی A4 جا بشه. این رو با محاسبه‌ی دقیق خطوط طراحی کردم ولی چون امکان رندر واقعی PDF رو توی محیط خودم ندارم، لطفاً یک‌بار پرینت واقعی بگیر و مطمئن شو؛ اگه هنوز به صفحه‌ی دوم سرریز می‌کنه بگو تا بیشتر فشرده‌ش کنم.

### فایل‌های تغییر یافته
`ContractTemplates.ts`+test, `registration.css`

---

## ۷. تاریخ شروع، سن از روی سال تولد، راست‌چین

تاریخ شروع کلاس (اولین تکرار روز انتخابی بعد از ثبت‌نام)، حذف فیلد «سن» (محاسبه خودکار از سال تولد)، راست‌چین صریح روی کارت قرارداد.

## ۶. شماره ترم خودکار

هر ثبت‌نام = یک ترم؛ بر اساس تعداد ثبت‌نام‌های قبلیِ همون کدملی محاسبه می‌شه (فایل: `migrations/0009_add_term_number.sql`).

## ۵. قرارداد هنرجویی خودکار

تشخیص خودکار نوع قرارداد (استاندارد/کلاویه‌ای) از `coursePricingMap`، پر شدن خودکار مبلغ (تومان×۱۰=ریال) و مبلغ‌به‌حرف، نمایش و چاپ در پایان ثبت‌نام (فایل: `migrations/0008_add_contract_fields.sql`).

## ۱-۴ (خلاصه)

auto-skip مراحل ثبت‌نام + لینک مستقیم · یکپارچه‌سازی استایل گواهینامه/کتاب · تشخیص خودکار کد ملی در فرم گواهینامه · بج ثبت‌نام روی کارت‌های دوره و استاد

---

## تأیید نهایی

- `npm run test` → ۷۰/۷۰ تست
- `npm run check` → همون ۹ خطای از قبل موجود و بی‌ربط، صفر خطای جدید
- `npm run build` → کامل و بدون خطا
- خروجی واقعی قرارداد رو با داده‌های نمونه‌ی خودت (خلیل دلاوران) دوباره تولید و چک کردم — شماره موبایل و مبلغ درست، بدون معکوس‌شدگی

`package-lock.json` توی این zip نیست.

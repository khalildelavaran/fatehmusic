# تغییرات این بسته

همه فایل‌ها raw هستن (نه patch) — چون محیط ویندوزی با CRLF مشکل پچ داره.
مسیر هر فایل داخل zip دقیقاً همون مسیرش توی ریپو هست؛ فقط جایگزین کن.

**قبل از دیپلوی: دو migration رو به ترتیب اجرا کن** (`npm run db:migrate:local` برای تست، بعد `npm run db:migrate:remote`).

---

## ۹. صفحه‌ی دوم خالی و هدردار (جدیدترین بخش، مهم — لطفاً چک کن)

### تشخیص
اینکه متن کامل توی صفحه‌ی اول جا می‌شه و فضای خالی هم باقی می‌مونه، ولی هنوز یک صفحه‌ی دوم خالیِ «هدردار» ساخته می‌شه، دقیقاً امضای یک چیز شناخته‌شده‌ست: **تنظیم «سربرگ و پابرگ» (Headers and footers) توی پنجره‌ی چاپ مرورگر**. وقتی این گزینه فعاله، مرورگر (مستقل از `@page margin` که من توی CSS تنظیم کردم) یه فضای اضافه‌ی خودش رو بالا و پایین هر صفحه رزرو می‌کنه برای عنوان صفحه/تاریخ/URL — و همین فضای اضافه (که از دید CSS من قابل کنترل نیست) می‌تونه باعث بشه محتوا از دید *مرورگر* کمی سرریز کنه، حتی اگه از دید من جا می‌شه، و یک صفحه‌ی دوم تقریباً خالی با همون سربرگ مرورگر (نه چیزی از خود قرارداد) بسازه.

### لطفاً این رو تست کن
توی پنجره‌ی چاپ (Ctrl+P)، بخش «بیشتر» یا «تنظیمات بیشتر» رو باز کن و **«سربرگ و پابرگ» / «Headers and footers»** رو خاموش کن. تقریباً مطمئنم مشکل با همین حل می‌شه.

### چیزی که من هم اضافه کردم (مستقل از تنظیم بالا)
برای اطمینان بیشتر، فاصله‌ها و فونت رو یک درجه دیگه فشرده‌تر کردم (فونت ۹pt، فاصله‌ی کمتر بین مواد)، و به بلوک امضاء گفتم هیچ‌وقت وسط دو صفحه نصف نشه (`break-inside: avoid`) — چون یه دلیل شناخته‌شده‌ی دیگه برای این مشکل اینه که مرورگرها گاهی یک بلوک flex (مثل ردیف امضاء) رو اگه کامل جا نشه، به‌جای شکستنش، کامل می‌فرستن صفحه‌ی بعد. یک قانون هم اضافه کردم که هیچ فاصله‌ی اضافه‌ی ته‌مانده بعد از آخرین عنصر قرارداد نمونه.

فایل: `registration.css`

---

## ۸. بهبود پرینت قرارداد

متن اضافی بالای صفحه حذف شد (مال `.registration-hero` بود)، شماره موبایل و مبلغ به رقم فارسی بدون جداکننده (باگ RTL که برعکس نشون می‌داد رفع شد)، و بررسی «اطلاق» (سورس درسته؛ جابه‌جایی حروف احتمالاً از کپی‌کردن متن از PDF چاپ‌شده‌ست، نه از رندر واقعی صفحه).

## ۷. تاریخ شروع، سن از روی سال تولد، راست‌چین

تاریخ شروع کلاس (اولین تکرار روز انتخابی بعد از ثبت‌نام)، حذف فیلد «سن» (محاسبه خودکار از سال تولد)، راست‌چین صریح روی کارت قرارداد.

## ۶. شماره ترم خودکار

هر ثبت‌نام = یک ترم؛ بر اساس تعداد ثبت‌نام‌های قبلیِ همون کدملی محاسبه می‌شه (فایل: `migrations/0009_add_term_number.sql`).

## ۵. قرارداد هنرجویی خودکار

تشخیص خودکار نوع قرارداد، پر شدن خودکار مبلغ و مبلغ‌به‌حرف، نمایش و چاپ در پایان ثبت‌نام (فایل: `migrations/0008_add_contract_fields.sql`).

## ۱-۴ (خلاصه)

auto-skip مراحل ثبت‌نام + لینک مستقیم · یکپارچه‌سازی استایل گواهینامه/کتاب · تشخیص خودکار کد ملی در فرم گواهینامه · بج ثبت‌نام روی کارت‌های دوره و استاد

---

## تأیید نهایی

- `npm run test` → ۷۰/۷۰ تست
- `npm run check` → همون ۹ خطای از قبل موجود و بی‌ربط، صفر خطای جدید
- `npm run build` → کامل و بدون خطا (CSS رو هم از نظر syntax چک کردم چون توی این ویرایش یه آکولاد اضافه گذاشته بودم که خودم قبل تحویل پیدا و رفع کردم)

`package-lock.json` توی این zip نیست.

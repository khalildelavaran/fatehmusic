import { formatJalaliDate, formatJalaliTime } from "../../utils/format-date";
const liveStatus=document.querySelector("#liveStatus"),printButton=document.querySelector("#printRegistrations"),body=document.querySelector("#registrationsBody");
const statusLabels={pending:"در انتظار",contacted:"تماس گرفته شد",confirmed:"تأیید شد",cancelled:"لغو شد"};const headers=()=>({"Content-Type":"application/json"});const POLL_INTERVAL_MS=30000;let pollTimer=null;
function setLiveStatus(t){if(liveStatus)liveStatus.textContent=t}
async function loadRegistrations({silent=false}={}){if(!silent)setLiveStatus("در حال به‌روزرسانی...");let response;try{response=await fetch("/api/admin/registrations",{credentials:"same-origin",headers:headers()})}catch{setLiveStatus("خطا در ارتباط با سرور. تلاش مجدد در ۳۰ ثانیه...");return}if(response.status===401){location.assign("/admin/login");return}const data=await response.json();if(!data.success){setLiveStatus(data.message);return}body.innerHTML=data.registrations.length===0?`<tr><td colspan="9" class="admin-table-empty">هنوز ثبت‌نامی وجود ندارد.</td></tr>`:data.registrations.map(item=>`<tr><td>${item.tracking_code}</td><td>${item.student_first_name} ${item.student_last_name}<br><span class="admin-table-subtext">سن: ${item.student_age}</span></td><td dir="ltr">${item.student_national_code||"-"}</td><td dir="ltr">${item.student_mobile}</td><td>${item.instrument_title}</td><td>${item.instructor_name}</td><td>${item.schedule_weekday}<br><span class="admin-table-subtext">${item.schedule_duration||""} دقیقه · ${formatJalaliDate(item.created_at)}</span></td><td><span class="admin-status-pill" data-status="${item.status}">${statusLabels[item.status]||item.status}</span></td><td class="no-print admin-registration-actions"><select data-id="${item.id}">${Object.entries(statusLabels).map(([v,l])=>`<option value="${v}" ${item.status===v?"selected":""}>${l}</option>`).join("")}</select><button type="button" class="admin-document-btn" data-print-contract="${item.id}">چاپ قرارداد</button><a class="admin-document-btn" href="/admin/certificates?registration_id=${encodeURIComponent(item.id)}">گواهینامه</a>${item.student_national_code?`<button type="button" class="admin-document-btn" data-reset-student="${item.student_national_code}">بازنشانی رمز</button>`:""}</td></tr>`).join("");setLiveStatus(`${data.registrations.length} ثبت‌نام · آخرین به‌روزرسانی: ${formatJalaliTime(new Date())}`)}
function startPolling(){stopPolling();pollTimer=setInterval(()=>loadRegistrations({silent:true}),POLL_INTERVAL_MS)}function stopPolling(){if(pollTimer)clearInterval(pollTimer);pollTimer=null}document.addEventListener("visibilitychange",()=>{if(document.hidden)stopPolling();else{loadRegistrations({silent:true});startPolling()}});printButton?.addEventListener("click",()=>window.print());
body.addEventListener("click",async event=>{const reset=event.target.closest("[data-reset-student]");if(reset instanceof HTMLButtonElement){if(!confirm("رمز این هنرجو به کد ملی بازگردانده شود؟"))return;reset.disabled=true;const r=await fetch("/api/admin/student-reset-password",{method:"POST",headers:headers(),credentials:"same-origin",body:JSON.stringify({national_code:reset.dataset.resetStudent})});const d=await r.json().catch(()=>({message:"خطا"}));setLiveStatus(d.message||"انجام شد.");reset.disabled=false;return}const target=event.target.closest("[data-print-contract]");if(!(target instanceof HTMLButtonElement))return;const id=Number(target.dataset.printContract);if(!id)return;const tab=window.open("about:blank","_blank");if(!tab){setLiveStatus("مرورگر بازشدن تب جدید را مسدود کرده است.");return}tab.document.title="در حال آماده‌سازی قرارداد...";tab.document.body.innerHTML='<div style="font-family:Tahoma,sans-serif;direction:rtl;text-align:center;padding:48px">در حال تولید PDF قرارداد...</div>';target.disabled=true;const old=target.textContent;target.textContent="در حال تولید...";try{const r=await fetch("/api/admin/contract-generate",{method:"POST",headers:headers(),credentials:"same-origin",body:JSON.stringify({registration_id:id})});if(r.status===401){tab.close();location.assign("/admin/login");return}if(!r.ok){const d=await r.json().catch(()=>({message:"تولید قرارداد شکست خورد."}));tab.close();setLiveStatus(d.message);return}const u=URL.createObjectURL(await r.blob());tab.location.replace(u);tab.focus();setTimeout(()=>URL.revokeObjectURL(u),60000);setLiveStatus("PDF قرارداد در تب جدید باز شد.")}catch{tab.close();setLiveStatus("خطای شبکه هنگام تولید قرارداد.")}finally{target.disabled=false;target.textContent=old}});
body.addEventListener("change",async event=>{const target=event.target;if(!(target instanceof HTMLSelectElement))return;const r=await fetch("/api/admin/registrations",{method:"PATCH",headers:headers(),credentials:"same-origin",body:JSON.stringify({id:Number(target.dataset.id),status:target.value})});if(r.status===401){location.assign("/admin/login");return}const d=await r.json();if(!d.success)setLiveStatus(d.message);await loadRegistrations({silent:true})});loadRegistrations();startPolling();

(()=>{
  if(location.pathname!=="/admin/finance")return;
  const tab=document.querySelector("#studentsTab"),dateInput=document.querySelector("#financeDate");
  if(!tab||!dateInput||document.querySelector("#studentAccountLedger"))return;
  const money=v=>Number(v??0).toLocaleString("fa-IR"),esc=v=>{const d=document.createElement("div");d.textContent=String(v??"");return d.innerHTML;};
  const methods={cash:"نقدی",pos:"کارتخوان",transfer:"انتقال",online:"آنلاین",other:"سایر"};
  const state={selectedId:null,rows:[],lastDate:""};
  const style=document.createElement("style");style.textContent=`#studentAccountLedger{margin-top:16px}.sal-layout{display:grid;grid-template-columns:300px minmax(0,1fr);gap:16px}.sal-panel{background:var(--surface);border:1px solid var(--border);border-radius:14px;padding:16px}.sal-search{display:flex;gap:8px}.sal-search input{flex:1;border:1px solid var(--border);background:var(--surface);color:var(--text);border-radius:9px;padding:9px;font:inherit}.sal-list{margin-top:10px;max-height:560px;overflow:auto}.sal-student{display:block;width:100%;text-align:right;border:1px solid var(--border);background:transparent;color:var(--text);border-radius:10px;padding:10px;margin-bottom:7px;cursor:pointer;font:inherit}.sal-student.is-active{border-color:var(--gold-light);background:rgba(212,175,55,.07)}.sal-student small{display:block;opacity:.6;margin-top:3px}.sal-head{display:flex;align-items:center;justify-content:space-between;gap:12px}.sal-head h2{margin:0 0 7px;font-size:18px}.sal-status{display:inline-block;padding:4px 9px;border-radius:999px;border:1px solid var(--border);font-size:11px}.sal-status.debtor{border-color:#a33}.sal-status.creditor{border-color:#2f8f46}.sal-status.settled{border-color:#b89500}.sal-cards{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:14px 0}.sal-card{border:1px solid var(--border);border-radius:11px;padding:12px}.sal-card span{display:block;font-size:11px;opacity:.65}.sal-card b{display:block;margin-top:5px;font-size:18px}.sal-card.debt{border-color:#a33}.sal-card.credit{border-color:#2f8f46}.sal-table{width:100%;border-collapse:collapse;font-size:12px}.sal-table th,.sal-table td{padding:8px;border-bottom:1px solid var(--border);text-align:right;white-space:nowrap}.sal-table th{opacity:.65}.sal-table-wrap{overflow:auto}.sal-section{margin-top:16px}.sal-note{font-size:11px;opacity:.6;margin-top:10px}.sal-btn{border:1px solid var(--border);background:var(--surface);color:var(--text);border-radius:9px;padding:8px 12px;font:inherit;font-weight:800;cursor:pointer}.sal-pay-btn{background:var(--primary);border-color:var(--primary);color:#fff}.sal-payment-form{display:none;margin-top:8px;padding:9px;border:1px solid var(--border);border-radius:9px;background:rgba(212,175,55,.04)}.sal-payment-form.is-open{display:flex;align-items:center;gap:7px;flex-wrap:wrap}.sal-payment-form input,.sal-payment-form select{border:1px solid var(--border);background:var(--surface);color:var(--text);border-radius:8px;padding:7px;font:inherit}.sal-payment-form input{width:120px}.sal-payment-status{font-size:11px}.sal-payment-status.err{color:#d77}.sal-payment-status.ok{color:#6bcf7a}.sal-debit{color:#d77}.sal-credit{color:#6bcf7a}@media(max-width:1000px){.sal-layout{grid-template-columns:1fr}.sal-cards{grid-template-columns:repeat(2,1fr)}}@media(max-width:600px){.sal-cards{grid-template-columns:1fr}.sal-head{align-items:flex-start;flex-direction:column}.sal-payment-form.is-open{align-items:stretch}.sal-payment-form input,.sal-payment-form select,.sal-payment-form button{width:100%;box-sizing:border-box}}`;
  document.head.appendChild(style);
  const section=document.createElement("section");section.id="studentAccountLedger";section.innerHTML=`<div class="sal-layout"><article class="sal-panel"><h2>انتخاب هنرجو</h2><div class="sal-search"><input id="salSearch" placeholder="جستجوی نام هنرجو" autocomplete="off"><button id="salSearchBtn" class="sal-btn" type="button">جستجو</button></div><div id="salList" class="sal-list"></div></article><section id="salAccount" class="sal-panel"><div class="empty">یک هنرجو را انتخاب کنید.</div></section></div>`;tab.appendChild(section);
  const list=document.querySelector("#salList"),account=document.querySelector("#salAccount"),search=document.querySelector("#salSearch");
  const currentDate=()=>dateInput.dataset.iso||new Date().toISOString().slice(0,10);
  const json=async r=>{const t=await r.text();let d;try{d=JSON.parse(t)}catch{throw Error(`پاسخ نامعتبر از سرور (${r.status})`)}if(!r.ok||!d.success)throw Error(d.message||"خطای سرور");return d;};
  function renderList(){const q=search.value.trim().toLowerCase();const rows=state.rows.filter(x=>String(x.studentName).toLowerCase().includes(q));list.innerHTML=rows.length?rows.map(x=>`<button class="sal-student ${x.studentId===state.selectedId?"is-active":""}" type="button" data-id="${Number(x.studentId)}"><b>${esc(x.studentName)}</b><small>${esc(x.classTitle||"—")} · مانده ${money(x.balance)} تومان</small></button>`).join(""):"<div class=empty>هنرجویی پیدا نشد.</div>";}
  async function loadRows(){try{const r=await fetch(`/api/admin/finance-students?date=${encodeURIComponent(currentDate())}`,{credentials:"same-origin",headers:{Accept:"application/json"}});const d=await json(r);state.rows=d.students||[];renderList();if(state.selectedId&&state.rows.some(x=>x.studentId===state.selectedId))await loadAccount(state.selectedId);else if(state.rows[0])await loadAccount(state.rows[0].studentId);}catch(e){account.innerHTML=`<div class="finance-error">${esc(e instanceof Error?e.message:"دریافت حساب هنرجویان ناموفق بود.")}</div>`;}}
  async function submitPayment(invoiceId,form){const amount=Number(form.querySelector("[data-payment-amount]").value),method=form.querySelector("[data-payment-method]").value,reference=form.querySelector("[data-payment-reference]").value,note=form.querySelector("[data-payment-note]").value,status=form.querySelector("[data-payment-status]");if(!Number.isInteger(amount)||amount<=0){status.className="sal-payment-status err";status.textContent="مبلغ پرداخت را صحیح وارد کنید.";return}status.className="sal-payment-status";status.textContent="در حال ثبت...";try{const r=await fetch("/api/admin/payments",{method:"POST",credentials:"same-origin",headers:{"Content-Type":"application/json",Accept:"application/json"},body:JSON.stringify({invoiceId,amount,method,reference,note})});const d=await json(r);status.className="sal-payment-status ok";status.textContent="پرداخت با موفقیت ثبت شد.";await loadAccount(state.selectedId);await loadRows();}catch(e){status.className="sal-payment-status err";status.textContent=e instanceof Error?e.message:"ثبت پرداخت ناموفق بود.";}}
  async function loadAccount(id){state.selectedId=Number(id);renderList();account.innerHTML='<div class="empty">در حال دریافت گردش حساب...</div>';try{const r=await fetch(`/api/admin/student-account?studentId=${id}&date=${encodeURIComponent(currentDate())}`,{credentials:"same-origin",headers:{Accept:"application/json"}});const d=await json(r);const s=d.summary||{},st=d.student||{},terms=d.terms||[],payments=d.payments||[];const net=Number(s.totalInvoiced||0)-Number(s.totalPaid||0),debit=Math.max(net,0),credit=Math.max(-net,0);const status=debit>0?"debtor":credit>0?"creditor":"settled";const statusText=status==="debtor"?"بدهکار":status==="creditor"?"بستانکار":"تسویه";const ledger=[];terms.forEach(t=>{if(Number(t.invoiceAmount)>0)ledger.push({date:t.startDate,type:"debit",title:`شهریه ترم ${t.termNumber}`,detail:t.classTitle||"—",amount:Number(t.invoiceAmount),reference:""});});payments.forEach(p=>ledger.push({date:String(p.paidAt||"").slice(0,10),type:"credit",title:"پرداخت شهریه",detail:`${p.classTitle||"—"} · ترم ${p.termNumber||"—"}`,amount:Number(p.amount),reference:p.reference||""}));ledger.sort((a,b)=>String(a.date).localeCompare(String(b.date))||(({debit:0,credit:1}[a.type])-({debit:0,credit:1}[b.type])));let running=0;ledger.forEach(x=>{running+=x.type==="debit"?x.amount:-x.amount;x.running=running;});
      account.innerHTML=`<div class="sal-head"><div><h2>${esc(st.name)}</h2><span class="sal-status ${status}">${statusText}</span></div><button class="sal-btn" type="button" onclick="window.print()">پرینت گردش حساب</button></div><div class="sal-cards"><div class="sal-card"><span>جمع بدهکار</span><b>${money(s.totalInvoiced)}</b></div><div class="sal-card credit"><span>جمع بستانکار</span><b>${money(s.totalPaid)}</b></div><div class="sal-card ${debit>0?"debt":credit>0?"credit":""}"><span>${debit>0?"مانده بدهکار":credit>0?"مانده بستانکار":"مانده حساب"}</span><b>${money(debit||credit)}</b></div><div class="sal-card"><span>تعداد پرداخت</span><b>${money(payments.length)}</b></div></div><article class="sal-panel sal-section"><h3>گردش حساب</h3><div class="sal-table-wrap"><table class="sal-table"><thead><tr><th>تاریخ</th><th>شرح</th><th>کلاس/ترم</th><th>بدهکار</th><th>بستانکار</th><th>مانده</th></tr></thead><tbody>${ledger.length?ledger.map(x=>`<tr><td>${esc(x.date||"—")}</td><td>${esc(x.title)}</td><td>${esc(x.detail)}</td><td class="sal-debit">${x.type==="debit"?money(x.amount):"—"}</td><td class="sal-credit">${x.type==="credit"?money(x.amount):"—"}</td><td>${money(Math.abs(x.running))} ${x.running>0?"بدهکار":x.running<0?"بستانکار":"تسویه"}</td></tr>`).join(""):"<tr><td colspan=\"6\">گردش حسابی ثبت نشده است.</td></tr>"}</tbody></table></div></article><article class="sal-panel sal-section"><h3>حساب شهریه دوره‌ها</h3><div class="sal-table-wrap"><table class="sal-table"><thead><tr><th>کلاس</th><th>ترم</th><th>شهریه</th><th>پرداخت</th><th>مانده</th><th>وضعیت</th><th>عملیات</th></tr></thead><tbody>${terms.map(x=>`<tr><td>${esc(x.classTitle)}</td><td>${money(x.termNumber)}</td><td>${money(x.invoiceAmount)}</td><td>${money(x.paidAmount)}</td><td>${money(x.balance)}</td><td>${esc(x.financialStatus||"بدون صورتحساب")}</td><td>${x.invoiceId&&Number(x.balance)>0?`<button type="button" class="sal-btn sal-pay-btn" data-open-payment="${Number(x.invoiceId)}">ثبت پرداخت</button><form class="sal-payment-form" data-payment-form="${Number(x.invoiceId)}"><input data-payment-amount type="number" min="1" max="${Number(x.balance)}" step="1" placeholder="مبلغ"><select data-payment-method><option value="">روش پرداخت</option><option value="cash">نقدی</option><option value="pos">کارتخوان</option><option value="transfer">انتقال</option><option value="online">آنلاین</option><option value="other">سایر</option></select><input data-payment-reference maxlength="200" placeholder="شماره پیگیری"><input data-payment-note maxlength="500" placeholder="یادداشت"><button type="submit" class="sal-btn sal-pay-btn">ثبت</button><span data-payment-status class="sal-payment-status"></span></form>`:"—"}</td></tr>`).join("")||"<tr><td colspan=\"7\">دوره‌ای ثبت نشده است.</td></tr>"}</tbody></table></div></article><article class="sal-panel sal-section"><h3>سابقه پرداخت‌ها</h3><div class="sal-table-wrap"><table class="sal-table"><thead><tr><th>تاریخ</th><th>مبلغ</th><th>روش</th><th>کلاس</th><th>ترم</th><th>شماره پیگیری</th><th>یادداشت</th></tr></thead><tbody>${payments.map(p=>`<tr><td>${esc(String(p.paidAt||"").replace("T"," ").slice(0,16))}</td><td>${money(p.amount)}</td><td>${esc(methods[p.method]||p.method||"—")}</td><td>${esc(p.classTitle||"—")}</td><td>${money(p.termNumber)}</td><td>${esc(p.reference||"—")}</td><td>${esc(p.note||"—")}</td></tr>`).join("")||"<tr><td colspan=\"7\">پرداختی ثبت نشده است.</td></tr>"}</tbody></table></div></article><div class="sal-note">بدهکار یعنی مجموع شهریه‌های ثبت‌شده بیشتر از پرداخت‌هاست؛ بستانکار یعنی پرداخت‌ها بیشتر از بدهی ثبت‌شده است.</div>`;
    }catch(e){account.innerHTML=`<div class="finance-error">${esc(e instanceof Error?e.message:"دریافت گردش حساب ناموفق بود.")}</div>`;}}
  list.addEventListener("click",e=>{const b=e.target.closest("[data-id]");if(b)loadAccount(Number(b.dataset.id));});account.addEventListener("click",e=>{const b=e.target.closest("[data-open-payment]");if(!b)return;const f=account.querySelector(`[data-payment-form="${b.dataset.openPayment}"]`);if(f)f.classList.toggle("is-open");});account.addEventListener("submit",e=>{const f=e.target.closest("form[data-payment-form]");if(f){e.preventDefault();submitPayment(Number(f.dataset.paymentForm),f);}});search.addEventListener("input",renderList);document.querySelector("#salSearchBtn").addEventListener("click",renderList);const observer=new MutationObserver(()=>{const d=dateInput.dataset.iso||"";if(d!==state.lastDate){state.lastDate=d;loadRows();}});observer.observe(dateInput,{attributes:true,attributeFilter:["data-iso"]});state.lastDate=dateInput.dataset.iso||"";loadRows();
})();
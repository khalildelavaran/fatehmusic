(() => {
  if (location.pathname !== '/admin/daily') return;
  const root = document.querySelector('#dailyDashboardRoot');
  const summary = document.querySelector('#summary');
  if (!root || !summary) return;
  const dateKey=(d=new Date())=>{const y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return `${y}-${m}-${day}`;};
  const state={date:dateKey(),dashboard:null,command:null,metrics:null,loading:false};
  const esc=v=>{const e=document.createElement('div');e.textContent=String(v??'');return e.innerHTML;};
  const num=v=>Number(v??0).toLocaleString('fa-IR'); const money=v=>`${num(v)} تومان`;
  const ids=v=>new Set((Array.isArray(v)?v:String(v??'').split(',')).map(x=>String(x).trim()).filter(Boolean));
  function ensureUi(){if(document.querySelector('#dailyActionCenter'))return;summary.insertAdjacentHTML('afterend',`<section id="dailyActionCenter" class="dcc-action-center" aria-label="نیازمند اقدام"></section><section id="dailySearch" class="dcc-search" aria-label="جستجوی سریع"><div class="dcc-section-head"><div><strong>جستجوی سریع</strong><span>هنرجو، استاد یا کلاس را جستجو کنید.</span></div></div><div class="dcc-search-row"><input id="dccSearchInput" type="search" autocomplete="off" placeholder="مثلاً: علی احمدی، استاد محمدی، گیتار" aria-label="جستجوی هنرجو، استاد یا کلاس"/><button id="dccSearchButton" type="button">جستجو</button></div><div id="dccSearchResults" class="dcc-search-results" aria-live="polite"></div></section><section id="dailyInstructorStatus" class="dcc-instructors" aria-label="وضعیت استادان امروز"></section>`);}
  async function getDashboard(date){const r=await fetch(`/api/admin/daily-dashboard?date=${encodeURIComponent(date)}`,{credentials:'same-origin',headers:{Accept:'application/json'}});const d=await r.json();if(!r.ok||!d.success)throw Error(d.message||'خطا در دریافت جلسات');return d;}
  async function getCommand(date,q=''){const p=new URLSearchParams({date});if(q)p.set('q',q);const r=await fetch(`/api/admin/daily-command-center?${p}`,{credentials:'same-origin',headers:{Accept:'application/json'}});const d=await r.json();if(!r.ok||!d.success)throw Error(d.message||'خطا در دریافت شاخص‌های عملیاتی');return d;}
  function renderSummary(){const sessions=(state.dashboard?.sessions||[]).filter(s=>s.status!=='cancelled'),m=state.metrics||{},totalStudents=Number(m.unique_students??0),present=Number(m.unique_present_students??0),pending=Number(m.unique_pending_students??0);const cards=[['جلسه امروز',num(sessions.length),''],['هنرجوی یکتا',num(totalStudents),pending?`${num(pending)} حضور ثبت نشده`:'حضورها ثبت شده'],['حضور',`${num(present)} / ${num(totalStudents)}`,''],['پرداخت امروز',money(m.paymentsToday),`${num(m.paymentCountToday)} تراکنش`],['بدهی باز',num(m.outstanding_terms),m.outstanding_terms?'شامل موارد سررسید نشده و معوق':'موردی نیست'],['معوقه',num(m.overdue_terms),m.overdue_terms?'نیازمند پیگیری':'موردی نیست'],['تمدید',num(m.renewalCandidates),m.renewalCandidates?'نزدیک به پایان / ماهانه':'موردی نیست']];summary.innerHTML=cards.map(([t,v,n])=>`<div class="dd-stat dcc-kpi"><strong>${v}</strong><span>${t}</span>${n?`<small>${esc(n)}</small>`:''}</div>`).join('');}
  function sessionIdSetForAction(label){const sessions=(state.dashboard?.sessions||[]).filter(s=>s.status!=='cancelled'),c=state.command||{};if(label.includes('تداخل زمانی مدرس'))return ids(c.instructor_conflict_session_ids);if(label.includes('تداخل زمانی اتاق'))return ids(c.room_conflict_session_ids);if(label.includes('بدون اتاق'))return new Set(sessions.filter(s=>s.room_id==null).map(s=>String(s.id)));if(label.includes('حضور استادان'))return new Set(sessions.filter(s=>(s.teacher_attendance_status||s.teacherAttendanceStatus||'pending')==='pending').map(s=>String(s.id)));if(label.includes('حضور هنرجویان'))return new Set(sessions.filter(s=>(s.students||[]).some(st=>st.attendanceStatus==='pending')).map(s=>String(s.id)));if(label.includes('معوقه'))return ids(c.overdue_session_ids);if(label.includes('پرداخت ناقص'))return ids(c.partial_payment_session_ids);if(label.includes('تمدید'))return ids(c.renewal_session_ids);return new Set();}
  function renderActions(){const el=document.querySelector('#dailyActionCenter');if(!el)return;const sessions=(state.dashboard?.sessions||[]).filter(s=>s.status!=='cancelled'),students=sessions.flatMap(s=>s.students||[]),m=state.metrics||{},pendingTeacher=sessions.filter(s=>(s.teacher_attendance_status||s.teacherAttendanceStatus||'pending')==='pending').length,withoutRoom=sessions.filter(s=>s.room_id==null).length;const items=[[students.filter(s=>s.attendanceStatus==='pending').length,'حضور هنرجویان ثبت نشده','attention'],[pendingTeacher,'حضور استادان ثبت نشده','attention'],[withoutRoom,'جلسه بدون اتاق','warning'],[Number(m.instructor_conflicts||0),'تداخل زمانی مدرس','danger'],[Number(m.room_conflicts||0),'تداخل زمانی اتاق','danger'],[Number(m.overdue_terms||0),'ترم دارای معوقه','warning'],[Number(m.partial_payment_terms||0),'پرداخت ناقص','warning'],[Number(m.renewalCandidates||0),'هنرجو/ترم نیازمند تمدید','attention']].filter(x=>x[0]>0);el.innerHTML=`<div class="dcc-section-head"><div><strong>نیازمند اقدام</strong><span>${items.length?'مواردی که بهتر است منشی امروز پیگیری کند':'برای امروز مورد فوری ثبت نشده است.'}</span></div><b>${num(items.reduce((a,x)=>a+Number(x[0]),0))}</b></div>`+(items.length?`<div class="dcc-action-grid">${items.map(([count,label,type])=>`<button type="button" class="dcc-action ${type}" data-dcc-focus="${esc(label)}"><strong>${num(count)}</strong><span>${esc(label)}</span></button>`).join('')}</div>`:`<div class="dcc-all-clear">✓ وضعیت عملیاتی امروز مرتب است.</div>`);}
  function renderInstructors(){const el=document.querySelector('#dailyInstructorStatus');if(!el)return;const rows=state.command?.instructors||[];el.innerHTML=`<div class="dcc-section-head"><div><strong>وضعیت استادان</strong><span>تعداد جلسات و وضعیت حضور هر استاد در روز انتخاب‌شده</span></div></div>`+(rows.length?`<div class="dcc-instructor-grid">${rows.map(r=>{const p=Number(r.teacher_pending||0),a=Number(r.teacher_absent||0),cls=a?'danger':p?'warning':'ok';return `<article class="dcc-instructor ${cls}"><strong>${esc(r.instructor_name||'استاد بدون نام')}</strong><span>${num(r.session_count)} جلسه · ${esc(r.first_session||'—')} تا ${esc(r.last_session||'—')}</span><small>${a?`غایب: ${num(a)}`:p?`حضور ثبت‌نشده: ${num(p)}`:'✓ حضور جلسات ثبت شده'}</small></article>`}).join('')}</div>`:`<div class="dcc-all-clear">استادی برای این روز جلسه ندارد.</div>`);}
  function fitTimelineRange(){const sessions=(state.dashboard?.sessions||[]).filter(s=>s.status!=='cancelled');const starts=sessions.map(s=>{const [h,m]=String(s.start_time||s.startTime||'00:00').split(':').map(Number);return h*60+m}).filter(Number.isFinite);const ends=sessions.map(s=>{const [h,m]=String(s.end_time||s.endTime||'00:00').split(':').map(Number);return h*60+m}).filter(Number.isFinite);const hours=document.querySelector('#timeline .dd-hours');const grid=document.querySelector('#timeline .dd-grid');if(!hours||!grid||!starts.length||!ends.length)return;const start=Math.max(0,Math.floor((Math.min(...starts)-30)/60)*60);const end=Math.min(1440,Math.ceil((Math.max(...ends)+30)/60)*60);const total=Math.max(60,end-start);const pxPerMinute=2.1;const trackWidth=total*pxPerMinute;hours.style.minWidth=`${trackWidth}px`;document.querySelectorAll('#timeline .dd-track').forEach(track=>{track.style.minWidth=`${trackWidth}px`;});const marks=[];for(let m=start;m<=end;m+=30){const left=((m-start)/total)*100;const major=m%60===0;marks.push(`<span class="dd-hour ${major?'dd-hour-major':'dd-hour-minor'}" style="left:${left}%">${major?String(Math.floor(m/60)).padStart(2,'0')+':00':''}</span>`);}hours.innerHTML=marks.join('');const byId=new Map(sessions.map(s=>[String(s.id),s]));document.querySelectorAll('#timeline .dd-card[data-session-id]').forEach(card=>{const s=byId.get(String(card.dataset.sessionId||''));if(!s)return;const parse=v=>{const [h,m]=String(v||'00:00').split(':').map(Number);return h*60+m};const a=parse(s.start_time||s.startTime),b=Math.max(a+15,parse(s.end_time||s.endTime));card.style.right=`${((a-start)/total)*100}%`;card.style.width=`${((b-a)/total)*100}%`;});}
  function decorateConflicts(){const instructor=ids(state.command?.instructor_conflict_session_ids),room=ids(state.command?.room_conflict_session_ids);document.querySelectorAll('#timeline [data-session-id],#sessionsPanel [data-session-id]').forEach(node=>{const id=String(node.dataset.sessionId||'');node.querySelectorAll('.dcc-conflict-badges').forEach(x=>x.remove());node.classList.toggle('has-instructor-conflict',instructor.has(id));node.classList.toggle('has-room-conflict',room.has(id));const badges=[];if(instructor.has(id))badges.push('<span class="dd-session-tag conflict-instructor">تداخل مدرس</span>');if(room.has(id))badges.push('<span class="dd-session-tag conflict-room">تداخل اتاق</span>');if(badges.length){const wrap=document.createElement('div');wrap.className='dcc-conflict-badges';wrap.innerHTML=badges.join('');node.prepend(wrap);}});}
  function renderNowLine(){const grid=document.querySelector('#timeline .dd-grid'),hours=document.querySelector('#timeline .dd-hours');if(!grid||!hours||state.date!==dateKey()){document.querySelector('#dccNowLine')?.remove();return;}const marks=[...hours.querySelectorAll('.dd-hour')];if(marks.length<2)return;const first=marks[0],last=marks[marks.length-1],parse=t=>{const m=String(t||'').match(/(\d{1,2}):(\d{2})/);return m?Number(m[1])*60+Number(m[2]):null;},a=parse(first.textContent),b=parse(last.textContent);if(a==null||b==null||b<=a){document.querySelector('#dccNowLine')?.remove();return;}const now=new Date(),mins=now.getHours()*60+now.getMinutes()+now.getSeconds()/60;if(mins<a||mins>b){document.querySelector('#dccNowLine')?.remove();return;}const startPct=Number.parseFloat(first.style.left)||0,endPct=Number.parseFloat(last.style.left)||100,pct=startPct+((mins-a)/(b-a))*(endPct-startPct);let line=document.querySelector('#dccNowLine');if(!line){line=document.createElement('div');line.id='dccNowLine';line.className='dcc-now-line';line.setAttribute('aria-hidden','true');grid.appendChild(line);}line.style.left=`${pct}%`;line.title=`اکنون ${now.toLocaleTimeString('fa-IR',{hour:'2-digit',minute:'2-digit'})}`;}
  function focus(label){const idsToFocus=sessionIdSetForAction(label),panels=[...document.querySelectorAll('#sessionsPanel [data-session-id]')],nodes=panels.filter(n=>idsToFocus.has(String(n.dataset.sessionId||'')));if(!nodes.length){document.querySelector('#sessionsPanel')?.scrollIntoView({behavior:'smooth',block:'start'});return;}nodes.forEach(n=>n.classList.add('dcc-focus'));nodes[0].scrollIntoView({behavior:'smooth',block:'center'});setTimeout(()=>nodes.forEach(n=>n.classList.remove('dcc-focus')),2200);}
  async function search(){const input=document.querySelector('#dccSearchInput'),out=document.querySelector('#dccSearchResults'),q=input?.value.trim()||'';if(!q){out.innerHTML='';return;}out.innerHTML='<div class="dcc-search-loading">در حال جستجو…</div>';try{const d=await getCommand(state.date,q);out.innerHTML=(d.results||[]).map(x=>`<button type="button" class="dcc-result" data-search-type="${esc(x.result_type)}" data-search-id="${esc(x.id)}" data-session-ids="${esc(x.session_ids||'')}" data-search-name="${esc(x.name)}"><strong>${esc(x.name)}</strong><span>${x.result_type==='student'?`هنرجو · ${esc(x.class_title||'')}`:x.result_type==='instructor'?'استاد':`کلاس · ${esc(x.class_title||'')}`}</span>${x.session_date?`<small>${esc(x.session_date)} · ${esc(x.start_time||'')}–${esc(x.end_time||'')}</small>`:''}</button>`).join('')||'<div class="dcc-search-empty">نتیجه‌ای پیدا نشد.</div>';}catch(e){out.innerHTML=`<div class="dcc-search-empty">${esc(e.message||e)}</div>`;}}
  function selectSearch(e){const b=e.target.closest('.dcc-result');if(!b)return;const idSet=ids(b.dataset.sessionIds),dom=[...document.querySelectorAll('#timeline [data-session-id],#sessionsPanel [data-session-id]')],nodes=dom.filter(n=>idSet.has(String(n.dataset.sessionId||'')));if(nodes.length){nodes.forEach(n=>n.classList.add('dcc-focus'));nodes[0].scrollIntoView({behavior:'smooth',block:'center'});setTimeout(()=>nodes.forEach(n=>n.classList.remove('dcc-focus')),2200);}else document.querySelector('#sessionsPanel')?.scrollIntoView({behavior:'smooth',block:'start'});}
  async function load(){ensureUi();try{state.loading=true;const[dashboard,command]=await Promise.all([getDashboard(state.date),getCommand(state.date)]);state.dashboard=dashboard;state.command=command;state.metrics=command.metrics||{};renderSummary();renderActions();renderInstructors();setTimeout(()=>{fitTimelineRange();decorateConflicts();renderNowLine();},0);}catch(e){console.error('[daily-command-center]',e);}finally{state.loading=false;}}
  ensureUi();document.querySelector('#dccSearchButton')?.addEventListener('click',search);document.querySelector('#dccSearchInput')?.addEventListener('keydown',e=>{if(e.key==='Enter')search();});document.querySelector('#dccSearchResults')?.addEventListener('click',selectSearch);document.querySelector('#dailyActionCenter')?.addEventListener('click',e=>{const b=e.target.closest('[data-dcc-focus]');if(b)focus(b.dataset.dccFocus||'');});['prev','next','today','refresh'].forEach(id=>document.querySelector(`#${id}`)?.addEventListener('click',()=>{if(id==='prev'){const d=new Date(state.date);d.setDate(d.getDate()-1);state.date=dateKey(d);}else if(id==='next'){const d=new Date(state.date);d.setDate(d.getDate()+1);state.date=dateKey(d);}else if(id==='today')state.date=dateKey();setTimeout(load,700);}));setInterval(()=>{fitTimelineRange();decorateConflicts();renderNowLine();},15000);load();
})();

/* Student-first operational view: each individual student gets an explicit row. */
(() => {
  if (location.pathname !== '/admin/daily') return;
  const root = document.querySelector('#dailyDashboardRoot');
  if (!root) return;
  const esc = v => { const e=document.createElement('div'); e.textContent=String(v??''); return e.innerHTML; };
  const today = () => { const d=new Date(); return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`; };
  let currentDate = today();
  let busy = false;
  const labels = { pending:'ثبت نشده', present:'حاضر', absent:'غایب', excused:'مرخصی', withdrawn:'انصراف' };
  const style = document.createElement('style');
  style.textContent = `
    #dailyStudentRoster{margin:18px 0;display:grid;gap:12px}
    .dsr-head{display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap;padding:16px 18px;border:1px solid var(--border);border-radius:16px;background:var(--surface)}
    .dsr-head strong{font-size:17px}.dsr-head span{display:block;color:var(--text-secondary);font-size:12px;margin-top:4px}
    .dsr-list{display:grid;gap:8px}.dsr-row{display:grid;grid-template-columns:minmax(180px,1.4fr) minmax(120px,.7fr) minmax(210px,1fr) minmax(150px,.8fr);gap:10px;align-items:center;padding:12px 14px;border:1px solid var(--border);border-radius:12px;background:var(--surface)}
    .dsr-name{font-weight:800}.dsr-class{font-size:12px;color:var(--text-secondary)}.dsr-time{font-weight:800;direction:ltr;text-align:center}.dsr-time button,.dsr-time input{font:inherit}.dsr-edit{display:inline-flex;gap:6px;align-items:center}.dsr-edit input{width:105px;padding:7px;border:1px solid var(--border);border-radius:8px;background:var(--surface);color:var(--text)}
    .dsr-attendance{display:flex;gap:5px;flex-wrap:wrap}.dsr-attendance button{border:1px solid var(--border);background:var(--surface);color:var(--text);border-radius:8px;padding:7px 9px;cursor:pointer}.dsr-attendance button.active{font-weight:800;border-color:currentColor}.dsr-attendance button.present{color:#2f8f46}.dsr-attendance button.absent{color:#d66666}.dsr-attendance button.excused{color:#3b82f6}.dsr-attendance button.withdrawn{color:var(--text-secondary)}
    .dsr-status{font-size:12px;color:var(--text-secondary)}
    @media (max-width:900px){.dsr-row{grid-template-columns:1fr 1fr}.dsr-attendance{grid-column:1/-1}.dsr-time{justify-self:start}}
    @media (max-width:560px){.dsr-row{grid-template-columns:1fr}.dsr-attendance{grid-column:auto}.dsr-edit{flex-wrap:wrap}}
  `;
  document.head.appendChild(style);
  const panel = document.createElement('section'); panel.id='dailyStudentRoster'; panel.setAttribute('aria-label','فهرست مستقل هنرجویان');
  const sessionsPanel=document.querySelector('#sessionsPanel');
  sessionsPanel?.parentNode?.insertBefore(panel,sessionsPanel);
  function time(v){return String(v||'').slice(0,5)}
  async function normalize(date){const r=await fetch(`/api/admin/daily-student-sessions?date=${encodeURIComponent(date)}`,{credentials:'same-origin',headers:{Accept:'application/json'}});const d=await r.json();if(!r.ok||!d.success)throw Error(d.message||'تفکیک جلسات هنرجویان ناموفق بود.');return d;}
  async function dashboard(date){const r=await fetch(`/api/admin/daily-dashboard?date=${encodeURIComponent(date)}&force=1`,{credentials:'same-origin',headers:{Accept:'application/json'}});const d=await r.json();if(!r.ok||!d.success)throw Error(d.message||'دریافت جلسات ناموفق بود.');return d;}
  function rows(data){const out=[];for(const s of data.sessions||[]){for(const st of s.students||[]){out.push({session:s,student:st});}}return out.sort((a,b)=>String(a.session.startTime).localeCompare(String(b.session.startTime))||String(a.student.studentName).localeCompare(String(b.student.studentName),'fa'));}
  function render(data,message=''){const all=rows(data);panel.innerHTML=`<div class="dsr-head"><div><strong>هنرجویان امروز</strong><span>هر هنرجو به‌صورت مستقل دیده می‌شود؛ زمان و وضعیت حضور از همین‌جا قابل تغییر است.</span></div><div class="dsr-status">${esc(message||`${all.length} هنرجو`)}</div></div><div class="dsr-list">${all.length?all.map(({session:s,student:st})=>{const individual=!['group','workshop'].includes(s.class_type);return `<article class="dsr-row" data-es-id="${esc(st.enrollmentSessionId)}" data-session-id="${esc(s.id)}"><div><div class="dsr-name">${esc(st.studentName||'هنرجوی بدون نام')}</div><div class="dsr-class">${esc(s.className||s.class_title||'کلاس')} · ${esc(s.instructorName||'')}</div></div><div class="dsr-time" data-time>${time(s.startTime)}–${time(s.endTime)}</div><div class="dsr-attendance">${['present','absent','excused','withdrawn'].map(v=>`<button type="button" class="${v} ${st.attendanceStatus===v?'active':''}" data-attendance="${v}" data-es-id="${esc(st.enrollmentSessionId)}" data-enrollment-id="${esc(st.enrollmentId)}">${labels[v]}</button>`).join('')}</div><div class="dsr-time-actions">${individual?`<button type="button" data-edit-time>تغییر ساعت</button>`:''}</div></article>`;}).join(''):'<div class="dd-empty-note">برای این روز هنرجویی ثبت نشده است.</div>'}</div>`;}
  async function load(date=currentDate){if(busy)return;busy=true;try{currentDate=date;const n=await normalize(date);const d=await dashboard(date);render(d,n.moved?`${num(n.moved)} جلسه هنرجو تفکیک شد.`:'همه جلسات هنرجویی مستقل هستند.');}catch(e){panel.innerHTML=`<div class="dsr-head"><div><strong>هنرجویان امروز</strong><span>${esc(e.message||e)}</span></div></div>`;}finally{busy=false;}}
  function num(v){return Number(v||0).toLocaleString('fa-IR');}
  panel.addEventListener('click',async e=>{const a=e.target.closest('[data-attendance]');if(a){a.disabled=true;try{const r=await fetch('/api/admin/daily-planner',{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json',Accept:'application/json'},body:JSON.stringify({enrollmentSessionId:Number(a.dataset.esId),enrollmentId:Number(a.dataset.enrollmentId),status:a.dataset.attendance})});const d=await r.json();if(!r.ok||!d.success)throw Error(d.message||'ثبت وضعیت حضور ناموفق بود.');await load(currentDate);document.querySelector('#refresh')?.click();}catch(err){a.disabled=false;alert(err.message||err);}}
    const edit=e.target.closest('[data-edit-time]');if(edit){const row=edit.closest('.dsr-row');const id=row.dataset.esId;const existing=row.querySelector('[data-time-edit]');if(existing)return;const current=row.querySelector('[data-time]').textContent.split('–');const box=document.createElement('div');box.className='dsr-edit';box.dataset.timeEdit='1';box.innerHTML=`<input type="time" value="${current[0]}" data-start step="300"><input type="time" value="${current[1]}" data-end step="300"><button type="button" data-save-time>保存</button><button type="button" data-cancel-time>انصراف</button>`;row.querySelector('[data-time]').replaceWith(box);}}
    const save=e.target.closest('[data-save-time]');if(save){const row=save.closest('.dsr-row'),box=row.querySelector('[data-time-edit]'),start=box.querySelector('[data-start]').value,end=box.querySelector('[data-end]').value;save.disabled=true;try{const r=await fetch('/api/admin/daily-student-sessions',{method:'PATCH',credentials:'same-origin',headers:{'content-type':'application/json',Accept:'application/json'},body:JSON.stringify({enrollmentSessionId:Number(row.dataset.esId),sessionDate:currentDate,startTime:start,endTime:end})});const d=await r.json();if(!r.ok||!d.success)throw Error(d.message||'ذخیره زمان هنرجو ناموفق بود.');await load(currentDate);document.querySelector('#refresh')?.click();}catch(err){save.disabled=false;alert(err.message||err);}}
    if(e.target.closest('[data-cancel-time]'))load(currentDate);
  });
  document.querySelectorAll('#prev,#next,#today,#refresh').forEach(b=>b.addEventListener('click',()=>{setTimeout(()=>{const text=document.querySelector('#dateLabel')?.textContent||'';if(b.id==='today')currentDate=today();else{const d=new Date(currentDate);if(b.id==='prev')d.setDate(d.getDate()-1);if(b.id==='next')d.setDate(d.getDate()+1);currentDate=dateKey(d);}load(currentDate);},850);}));
  load(currentDate);
})();